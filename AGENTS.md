# TIEFSTAPEL Agent Notes

- Mobile-first, dependency-free PWA in vanilla HTML/CSS/JavaScript.
- Original identity and graphics; do not use SKYJO logos or copied card art.
- Core rules live in `src/engine.js` and require automated tests.
- Run `npm test` and `npm run check` before delivery.
- Bump the service-worker cache version after app-shell changes.
